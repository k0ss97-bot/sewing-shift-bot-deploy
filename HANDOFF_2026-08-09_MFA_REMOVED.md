# Handoff для продолжения на другом ПК · 2026-08-09

## Актуальное состояние

- Production-сайт: `https://www.shagaemfabrika.ru/app`.
- Активный релиз: `/opt/sewing-web/releases/codex-f13e037-20260809T160722Z`.
- Production commit: `f13e037ecc0648a31d1bb970d301f73de401941a`.
- Рабочая ветка: `codex/article-first-wms`.
- Репозиторий: `https://github.com/k0ss97-bot/sewing-shift-bot-deploy.git`.
- Rollback-релиз: `/opt/sewing-web/releases/codex-beaa606-84b33884-20260809T111912Z`.
- Служба `sewing-web.service` активна; публичный `/health` возвращает `ready`.

## Последнее изменение

По прямому указанию владельца удалён обязательный MFA-вход администратора:

- экран настройки и ввода MFA удалён;
- TOTP-секрет больше не показывается в интерфейсе;
- администратор входит по логину и паролю;
- endpoint `POST /api/web/mfa/verify` удалён и на production возвращает HTTP 404;
- требование `mfa_verified` при восстановлении административной сессии удалено;
- сохранены проверка пароля, блокировка частых попыток, статусы аккаунтов, Secure-cookie,
  CSRF-защита, сроки сессий и разграничение ролей.

Старые MFA-поля SQLite оставлены неактивными для совместимости схемы. Миграция базы не
выполнялась, пользовательские и складские данные не менялись. Секрет, попавший на старый
скриншот, больше не используется опубликованным потоком входа и не должен применяться снова.

Изменённые файлы в commit `f13e037`:

- `miniapp_server.py`;
- `webapp_auth.py`;
- `assets/app/shell.html`;
- `assets/app/app.js`;
- `scripts/smoke_web.py`;
- `test_webapp_auth.py`.

## Проверки последнего изменения

- Python compile: 107 файлов, PASS.
- JavaScript syntax: PASS.
- Изолированный web-smoke локально: PASS.
- Изолированный web-smoke серверным Python 3.12 в release-кандидате: PASS.
- Локальный полный набор: 358 обнаружено, 342 выполнено и пройдено, 0 ошибок,
  16 PostgreSQL-тестов пропущены из-за отсутствующей локальной test-базы.
- Предыдущий полный gate на PostgreSQL 16 для функционального релиза: 358/358,
  ошибок и пропусков нет.
- Production после публикации: `/health` ready, новый fingerprinted JS отдаётся с HTTP 200,
  MFA-маркеры отсутствуют, старый MFA endpoint возвращает 404.

## Ранее завершённый функциональный этап

Production работает с PostgreSQL 16.14 и миграциями `001–018`. В интерфейсе доступен экран
`Админ → Планирование`:

- MRP и спецификации;
- мощности и навыки;
- OEE и качество;
- себестоимость и маржа;
- прогноз спроса;
- оптимизация размещения WMS.

Расчёты read-only: они не списывают остатки и не закрывают производственные задания.
После выкладки миграций были успешны production health, сверка производство→WMS и мониторинг.

## Резервные копии и известное ограничение

- SQLite: полный restore проверен, 73 таблицы.
- PostgreSQL: критическое восстановление схем и WMS-данных проверено — 12 миграций,
  15 зон, нарушений баланса нет.
- Полный второй restore PostgreSQL на том же сервере не помещается: рабочая база около 14 ГБ,
  свободного места было около 11–12 ГБ.
- Требуется расширить диск так, чтобы после live-базы и backup оставалось минимум 25–30 ГБ,
  либо использовать отдельный recovery-сервер. Старые backup ради места не удалялись.

## Получение проекта на другом ПК

```bash
git clone https://github.com/k0ss97-bot/sewing-shift-bot-deploy.git
cd sewing-shift-bot-deploy
git switch codex/article-first-wms
git pull --ff-only origin codex/article-first-wms
git rev-parse HEAD
```

Ожидаемый HEAD:

```text
f13e037ecc0648a31d1bb970d301f73de401941a
```

Не переносить между компьютерами `.env`, `bot.db`, `backups/`, `exports/` или `logs/`.
Они содержат runtime-данные и не должны попадать в Git.

## Безопасное продолжение

1. Сначала проверить `git status --short --branch` и точный HEAD.
2. Прочитать `AGENTS.md`, `DEPLOYMENT.md` и этот handoff.
3. Для тестов использовать только временный `DB_DIR`; не импортировать `database.py` рядом
   с рабочим `bot.db`.
4. Запускать узкую проверку, затем:

```bash
python3 scripts/check_python_compile.py
python3 scripts/run_unittests_isolated.py
python3 scripts/smoke_web.py
```

5. Не изменять production, базы, backup или реальные задания без отдельного прямого поручения.
6. Следующий инфраструктурный приоритет — организовать полный PostgreSQL recovery-test
   на расширенном диске или отдельном сервере.

