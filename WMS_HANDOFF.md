# WMS — актуальный handoff

Дата обновления: 2026-07-27. Рабочая ветка: `codex/wms-integration`.

## Что интегрировано

- WMS перенесена поверх актуального `main` (`5eb1263`).
- `miniapp_server.py` обслуживает `/api/wms/*` и проверяет сессию, CSRF и роль.
- Доступ имеют активный администратор и активный сотрудник, которому администратор включил дополнительное право «Доступ к складу». Основная должность сотрудника при этом не меняется.
- `employee_id` берётся только из аутентифицированной серверной сессии.
- Добавлены мобильные экраны: приёмка, размещение, выдача, перемещение,
  инвентаризация и списание.
- Администратор может создать ячейку и привязать товарный штрихкод из ТСД.
- Товарный штрихкод разрешается через `/api/wms/barcode/resolve`.
- Каждая мобильная операция передаёт уникальный `request_key`.
- Складской доступ реализован как дополнительная роль «Кладовщик»: основная
  производственная должность сотрудника не меняется.
- На мобильном и десктопе сотрудник с этой ролью переключает две полноценные
  рабочие среды: «Производство» и «Склад». Выбранная среда и последний
  производственный экран сохраняются на устройстве.
- В складской среде производственная навигация скрыта. Доступны складская
  главная, приёмка, размещение, перемещение, инвентаризация, списание,
  адресные остатки и история движений.
- Сотрудник без роли «Кладовщик» не видит переключатель и не может открыть WMS.
- Размещение работает строго по шагам: сканирование ячейки → сканирование
  товара → ввод количества → подтверждение.
- Выдача работает в обратном порядке: сканирование ячейки → просмотр её
  содержимого → сканирование товара → ввод количества → подтверждение.
- Поддерживаются камера телефона и аппаратный сканер ТСД, который передаёт код
  как клавиатурный ввод с Enter. Для ячеек используется код `LOC:<код ячейки>`.

## Исправления складского ядра

- Остаток хранится отдельно для каждой ячейки: уникальный ключ включает `location_id`.
- При первом переносе старые остатки помещаются в системную ячейку `RECEIVE-01`, откуда кладовщик может разнести их по физическим ячейкам через ТСД.
- Частичное размещение и перемещение больше не переносят весь остаток одной строкой.
- Добавлены ограничения `quantity >= 0` и `0 <= reserved_quantity <= quantity`.
- Postgres-соединения изолированы по потокам `ThreadingHTTPServer`.
- Репозиторий получает строки `DictCursor`, доступные и по индексу, и по имени поля.
- Исправлен вызов `repo.get_stock_rows()` в API.
- Исправлен SQLite cursor в bridge и конфликт по `legacy_sqlite_id`.
- Инвентаризация идемпотентна по `wms_inventory_counts.request_key`.
- Добавлена миграция `003_stock_per_location.sql` для уже созданной схемы.
- Seed создаёт системную ячейку приёмки `RECEIVE-01`.
- Выдача списывает остаток только из отсканированной ячейки и не позволяет
  затронуть зарезервированное количество.

## API

Запись (POST):

- `/api/wms/receive`
- `/api/wms/putaway`
- `/api/wms/transfer`
- `/api/wms/pick`
- `/api/wms/scrap`
- `/api/wms/inventory`
- `/api/wms/barcode/resolve`
- `/api/wms/barcode/register` — только администратор
- `/api/wms/locations/create` — только администратор

Чтение (GET или POST внутри текущего SPA):

- `/api/wms/locations`
- `/api/wms/stock`
- `/api/wms/movements`

## Production-состояние на 2026-07-27

- PostgreSQL 16 и отдельная база `sewing_wms` установлены на сервере приложения.
- Все три миграции применены, системные ячейки `RECEIVE-01` и `STORAGE-01` созданы.
- Первоначальный bridge выполнен: исходных складских строк было `0`, расхождений `0`.
- Отдельная тестовая PostgreSQL прошла все 27 WMS-тестов, включая выдачу из
  отсканированной ячейки и защиту резерва.
- Ежедневные проверяемые копии SQLite и PostgreSQL включены systemd-таймерами.
- Production работает на runtime-коммите `6db231d`: отдельная складская рабочая
  среда и пошаговые сценарии сканирования размещения/выдачи доступны через
  публичный `/app`, healthcheck успешен.

Администратору остаётся создать реальные физические ячейки вместо/в дополнение к
`STORAGE-01` и включить «Доступ к складу» только выбранным сотрудникам.

## Важная граница гибридной архитектуры

SQLite по-прежнему обслуживает производственные маршруты, а Postgres — адресное
хранение WMS. Bridge нельзя считать полноценной двусторонней транзакцией.
До окончательного перевода складских записей на один источник данных необходимо
контролировать сверку общих остатков и журналировать расхождения. Периодический
bridge не должен без проверки перезаписывать результаты операций ТСД.

## Проверки

```bash
python3 scripts/check_python_compile.py
python3 scripts/run_unittests_isolated.py
python3 scripts/smoke_web.py
```

Для полного локального набора использовался bundled Python с `openpyxl`:

```bash
/Users/konstantingorskih/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 scripts/run_unittests_isolated.py
```

Текущий результат: основной изолированный набор — 82 теста, 71 успешно и 11 HTTP
integration тестов ожидаемо пропущены; расширенный HTTP-набор — 82 из 82 успешно.
Web smoke, проверка JavaScript-синтаксиса и браузерный мобильный сценарий
«ячейка → товар → количество» успешны. На сервере основной изолированный набор
прошёл 82 теста без ошибок, а полный WMS-набор — 27 из 27 на отдельной временной
PostgreSQL. Postgres-тесты нельзя запускать против production.
